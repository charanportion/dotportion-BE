org: dotportion
service: dotportion-schema-api

custom:
  prune:
    automatic: true
    number: 2
  domains:
    dev: api-dev.dotportion.com
    prod: api.dotportion.com
    uat: api-uat.dotportion.com
  customDomain:
    domainName: ${self:custom.domains.${self:provider.stage}}
    stage: ${self:provider.stage}
    createRoute53Record: false
    basePath: "schemas"
    endpointType: "regional"
  authorizerArn: ${cf:dotportion-auth-api-${self:provider.stage}.AuthorizerArn}

provider:
  name: aws
  runtime: nodejs18.x
  region: ap-south-1
  stage: ${opt:stage, 'dev'}

  iamRoleStatements:
    - Effect: Allow
      Action:
        - "ssm:GetParameter"
      Resource: "arn:aws:ssm:${self:provider.region}:${aws:accountId}:parameter/dotportion/${self:provider.stage}/*"
    - Effect: Allow
      Action:
        - "lambda:InvokeFunction"
      Resource: ${self:custom.authorizerArn}

  environment:
    MONGO_URI: ${ssm:/dotportion/${self:provider.stage}/mongo_uri}
    MDataBase: ${self:provider.stage}
    BASE_URL: https://${self:custom.domains.${self:provider.stage}}

functions:
  createSchema:
    handler: create-schema.handler
    events:
      - http:
          path: /{tenant}/{projectId}
          method: post
          cors:
            origins:
              - "http://localhost:3000" # For local development
              - "https://www.dotportion.com/"
              - "https://no-code-api-builder-edby86tby-sri-charans-projects.vercel.app/"
              - "https://uat.dotportion.com"
              - "https://beta.dotportion.com"
              - "https://uat.d34dhbi3es40ie.amplifyapp.com"
              - "https://beta.d34dhbi3es40ie.amplifyapp.com"
            headers: # Optional: specify allowed headers
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
            allowCredentials: true
          authorizer:
            type: token
            identitySource: method.request.header.Authorization
            resultTtlInSeconds: 300 # Cache authorization for 5 minutes
            arn: ${self:custom.authorizerArn}

  getSchema:
    handler: get-schema.handler
    events:
      - http:
          path: /{tenant}/{projectId}/{collection}
          method: get
          cors:
            origins:
              - "http://localhost:3000" # For local development
              - "https://www.dotportion.com/"
              - "https://no-code-api-builder-edby86tby-sri-charans-projects.vercel.app/"
              - "https://uat.dotportion.com"
              - "https://beta.dotportion.com"
              - "https://uat.d34dhbi3es40ie.amplifyapp.com"
              - "https://beta.d34dhbi3es40ie.amplifyapp.com"
            headers: # Optional: specify allowed headers
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
            allowCredentials: true
          authorizer:
            type: token
            identitySource: method.request.header.Authorization
            resultTtlInSeconds: 300 # Cache authorization for 5 minutes
            arn: ${self:custom.authorizerArn}

  updateSchema:
    handler: update-schema.handler
    events:
      - http:
          path: /{tenant}/{projectId}/{collection}
          method: put
          cors:
            origins:
              - "http://localhost:3000" # For local development
              - "https://www.dotportion.com/"
              - "https://no-code-api-builder-edby86tby-sri-charans-projects.vercel.app/"
              - "https://uat.dotportion.com"
              - "https://beta.dotportion.com"
              - "https://uat.d34dhbi3es40ie.amplifyapp.com"
              - "https://beta.d34dhbi3es40ie.amplifyapp.com"
            headers: # Optional: specify allowed headers
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
            allowCredentials: true
          authorizer:
            type: token
            identitySource: method.request.header.Authorization
            resultTtlInSeconds: 300 # Cache authorization for 5 minutes
            arn: ${self:custom.authorizerArn}

  deleteSchema:
    handler: delete-schema.handler
    events:
      - http:
          path: /{tenant}/{projectId}/{collection}
          method: delete
          cors:
            origins:
              - "http://localhost:3000" # For local development
              - "https://www.dotportion.com/"
              - "https://no-code-api-builder-edby86tby-sri-charans-projects.vercel.app/"
              - "https://uat.dotportion.com"
              - "https://beta.dotportion.com"
              - "https://uat.d34dhbi3es40ie.amplifyapp.com"
              - "https://beta.d34dhbi3es40ie.amplifyapp.com"
            headers: # Optional: specify allowed headers
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
            allowCredentials: true
          authorizer:
            type: token
            identitySource: method.request.header.Authorization
            resultTtlInSeconds: 300 # Cache authorization for 5 minutes
            arn: ${self:custom.authorizerArn}

  getAvailableCollections:
    handler: get-available-collections.handler
    events:
      - http:
          path: /{tenant}/{projectId}/collections/all
          method: get
          cors:
            origins:
              - "http://localhost:3000" # For local development
              - "https://www.dotportion.com/"
              - "https://no-code-api-builder-edby86tby-sri-charans-projects.vercel.app/"
              - "https://uat.dotportion.com"
              - "https://beta.dotportion.com"
              - "https://uat.d34dhbi3es40ie.amplifyapp.com"
              - "https://beta.d34dhbi3es40ie.amplifyapp.com"
            headers: # Optional: specify allowed headers
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
            allowCredentials: true
          authorizer:
            type: token
            identitySource: method.request.header.Authorization
            resultTtlInSeconds: 300 # Cache authorization for 5 minutes
            arn: ${self:custom.authorizerArn}

  getCollectionParameters:
    handler: get-collection-parameters.handler
    events:
      - http:
          path: /{tenant}/{projectId}/collections/{collection}/parameters
          method: get
          cors:
            origins:
              - "http://localhost:3000" # For local development
              - "https://www.dotportion.com/"
              - "https://no-code-api-builder-edby86tby-sri-charans-projects.vercel.app/"
              - "https://uat.dotportion.com"
              - "https://beta.dotportion.com"
              - "https://uat.d34dhbi3es40ie.amplifyapp.com"
              - "https://beta.d34dhbi3es40ie.amplifyapp.com"
            headers: # Optional: specify allowed headers
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
            allowCredentials: true
          authorizer:
            type: token
            identitySource: method.request.header.Authorization
            resultTtlInSeconds: 300 # Cache authorization for 5 minutes
            arn: ${self:custom.authorizerArn}

  getAllCollectionParameters:
    handler: get-all-collection-parameters.handler
    events:
      - http:
          path: /{tenant}/{projectId}/collections/parameters
          method: get
          cors:
            origins:
              - "http://localhost:3000" # For local development
              - "https://www.dotportion.com/"
              - "https://no-code-api-builder-edby86tby-sri-charans-projects.vercel.app/"
              - "https://uat.dotportion.com"
              - "https://beta.dotportion.com"
              - "https://uat.d34dhbi3es40ie.amplifyapp.com"
              - "https://beta.d34dhbi3es40ie.amplifyapp.com"
            headers: # Optional: specify allowed headers
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
            allowCredentials: true
          authorizer:
            type: token
            identitySource: method.request.header.Authorization
            resultTtlInSeconds: 300 # Cache authorization for 5 minutes
            arn: ${self:custom.authorizerArn}

plugins:
  - serverless-offline
  - serverless-prune-plugin
  - serverless-domain-manager

# This tells the packaging process to exclude the AWS SDK, which is already available in the Lambda environment.
package:
  individually: true
  exclude:
    - node_modules/aws-sdk/**
